-- ==============================================================================
-- AUTO VOTE BEST CARD (META FARMING) - 100% FIXED BY DEBUGGER DATA
-- DENGAN FUNGSI KLIK PAKSA & DEBUG LOGGING INTENSIF
-- ==============================================================================

local Config = {
    EnableAutoVote = false, 
    
    PriorityCards = {
        "Tech Savvy",                         
        "TIME'S UP",                          
        "Well-Paced", "Endurance",            
        "Spare Change", "Penny Pincher",      
        "Avaricious", "Covetous",             
        "Lost and Found", "Blind Grab",       
        "Frugal",                             
        "Rehearsal", "Practice",              
        "Electrician",                        
        "Piping Tape",                        
        "Air Freshener",                      
        "Frost Shield",                       
        "Sparkplug",                          
        "First Aid", "Medical Attention",     
        "Suppression",                        
        "Decency", "Etiquette"                
    },
    
    VoteRandomIfNotFound = true 
}

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui") or Players.LocalPlayer:WaitForChild("PlayerGui")
local player = Players.LocalPlayer
local isVoting = false

-- ==================== GUI SETUP ====================
if CoreGui:FindFirstChild("AutoVoteGUI_Dandy") then
    CoreGui.AutoVoteGUI_Dandy:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AutoVoteGUI_Dandy"
screenGui.Parent = CoreGui

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 200, 0, 100)
mainFrame.Position = UDim2.new(0.5, -100, 0.8, -50)
mainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
mainFrame.Parent = screenGui
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 8)

local title = Instance.new("TextLabel", mainFrame)
title.Size = UDim2.new(1, 0, 0.4, 0)
title.BackgroundTransparency = 1
title.Text = "Auto Vote Card"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 16

local toggleBtn = Instance.new("TextButton", mainFrame)
toggleBtn.Size = UDim2.new(0.8, 0, 0.4, 0)
toggleBtn.Position = UDim2.new(0.1, 0, 0.45, 0)
toggleBtn.BackgroundColor3 = Config.EnableAutoVote and Color3.fromRGB(40, 200, 80) or Color3.fromRGB(220, 60, 60)
toggleBtn.Text = Config.EnableAutoVote and "ON" or "OFF"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 18
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 6)

-- Drag Logic
local dragging, dragStart, startPos
mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true dragStart = input.Position startPos = mainFrame.Position end
end)
mainFrame.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)
UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then local delta = input.Position - dragStart mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end)

toggleBtn.MouseButton1Click:Connect(function()
    Config.EnableAutoVote = not Config.EnableAutoVote
    toggleBtn.Text = Config.EnableAutoVote and "ON" or "OFF"
    toggleBtn.BackgroundColor3 = Config.EnableAutoVote and Color3.fromRGB(40, 200, 80) or Color3.fromRGB(220, 60, 60)
end)

-- ==================== AUTO VOTE LOGIC ====================
local function getCardPriorityWeight(cardName)
    local nameLower = string.lower(cardName)
    for i, priorityName in ipairs(Config.PriorityCards) do
        if string.find(nameLower, string.lower(priorityName)) then return i end
    end
    return 999 
end

-- FUNGSI KLIK PAKSA (Memaksakan Down -> Up -> Click)
local function executeForceClick(btnInstance, cardName)
    print("--------------------------------------------------")
    print("🛠️ Mencoba Klik Paksa: " .. cardName .. "...")
    
    if getconnections then
        local success = false
        
        -- 1. Picu MouseButton1Click (Cara Standar)
        local clickConns = getconnections(btnInstance.MouseButton1Click)
        for _, connection in pairs(clickConns) do
            connection:Fire()
            print("   ✅ MouseButton1Click dipicu.")
            success = true
        end
        
        -- 2. Picu Down/Up (Fallback kalau UI rumit)
        if #clickConns == 0 then
            print("   ⚠️ Peringatan: MouseButton1Click kosong. Mencoba fallback Down/Up...")
            
            for _, conn in pairs(getconnections(btnInstance.MouseButton1Down)) do
                conn:Fire()
                print("   ✅ MouseButton1Down dipicu.")
                success = true
            end
            task.wait(0.1) -- Jeda kecil biar game kerasa di-press
            for _, conn in pairs(getconnections(btnInstance.MouseButton1Up)) do
                conn:Fire()
                print("   ✅ MouseButton1Up dipicu.")
                success = true
            end
        end
        
        if success then
            print("🎉 [AUTO VOTE BERHASIL] Pemicu klik berhasil dieksekusi.")
        else
            print("❌ [CRITICAL] Gagal menemukan koneksi pemicu klik di tombol ini!")
        end
    else
        print("❌ [CRITICAL ERROR] Executor kamu TIDAK mendukung getconnections! Script auto-vote UI tidak mungkin bekerja.")
    end
    print("--------------------------------------------------")
end

task.spawn(function()
    while true do
        if Config.EnableAutoVote then
            pcall(function()
                local playerGui = player:WaitForChild("PlayerGui", 5)
                if playerGui then
                    local validVoteFrame = nil
                    
                    -- SCANNING FRAME VOTE
                    for _, gui in pairs(playerGui:GetChildren()) do
                        if gui:IsA("ScreenGui") and gui.Enabled and gui:FindFirstChild("VoteFrame", true) then
                            local vf = gui:FindFirstChild("VoteFrame", true)
                            if vf.Visible then
                                validVoteFrame = vf
                                break
                            end
                        end
                    end
                    
                    -- JIKA MENU VOTE TERBUKA
                    if validVoteFrame and not isVoting then
                        isVoting = true
                        print(">> VOTE FRAME TERDETEKSI! <<")
                        task.wait(2) -- Jeda biar render beres
                        
                        local bestCardName = nil
                        local bestCardBtn = nil
                        local bestWeight = 999
                        local firstAvailableCardName = nil
                        local firstAvailableCardBtn = nil
                        
                        -- LOG DEBUGGER INTENSIF
                        print("[DEBUG] Memulai Scanning Kartu...")
                        
                        for _, child in ipairs(validVoteFrame:GetChildren()) do
                            -- Gunakan validasi sesuai data Debugger (Visible TextButton & bukan Template)
                            if child:IsA("TextButton") and child.Visible and not string.find(child.Name, "Template") then
                                local itemNameLabel = child:FindFirstChild("ItemName", true)
                                
                                if itemNameLabel and itemNameLabel:IsA("TextLabel") then
                                    local cardName = itemNameLabel.Text
                                    local weight = getCardPriorityWeight(cardName)
                                    
                                    print("   [DEBUG KARTU] Ditemukan:", cardName, "| Prioritas Weight:", weight)
                                    
                                    if not firstAvailableCardName then
                                        firstAvailableCardName = cardName
                                        firstAvailableCardBtn = child
                                    end
                                    
                                    if weight < bestWeight then
                                        bestWeight = weight
                                        bestCardName = cardName
                                        bestCardBtn = child
                                    end
                                else
                                    print("   [DEBUG KARTU] TextButton '" .. child.Name .. "' tidak punya 'ItemName' label (Aneh).")
                                end
                            elseif child:IsA("TextButton") and not child.Visible then
                                -- Tampilkan untuk memverifikasi kita mengabaikan Template sampah
                                print("   [DEBUG KARTU] Mengabaikan kartu invisible:", child.Name)
                            end
                        end
                        
                        -- EKSEKUSI KEPUTUSAN
                        if bestCardBtn then
                            print("[DEBUG] Keputusan Akhir: Memilih PRIORITAS Terbaik ->", bestCardName)
                            executeForceClick(bestCardBtn, bestCardName)
                        elseif Config.VoteRandomIfNotFound and firstAvailableCardBtn then
                            print("[DEBUG] Keputusan Akhir: Prioritas kosong. Memilih acak pertama ->", firstAvailableCardName)
                            executeForceClick(firstAvailableCardBtn, firstAvailableCardName)
                        else
                            print("❌ [DEBUG ERROR] VoteFrame terbuka tapi tidak ada tombol kartu valid ditemukan.")
                        end
                        
                        task.wait(10) 
                        isVoting = false
                    end
                end
            end)
        end
        task.wait(1) 
    end
end)

print("✅ SCRIPT AUTO VOTE (FORCE-CLICK META-FARM) BERHASIL DIMUAT ✅")
print(">> Pastikan F9 terbuka untuk melihat Debug Logging.")
