-- ==============================================================================
-- CONFIGURATION: AUTO MACHINE FARMER V10 (DRAGGABLE GUI & AUTO DETECT SAFETY)
-- ==============================================================================
local Config = {
    Skillcheck = { Enable = false }, 
    Teleport = {
        Enable = true,          
        Mode = "Instant",       
        TweenSpeed = 1.25,      
        SafeRadius = 30,        
        PlayerRadius = 8,       
        DelayNext = 1.0,        
        UndergroundOffset = 11.5
    },
    ESP = {
        Enable = true,          
        NormalColor = Color3.fromRGB(150, 150, 150),   
        CompletedColor = Color3.fromRGB(255, 0, 255),  
        PossessedColor = Color3.fromRGB(0, 255, 255)   
    }
}
-- ==============================================================================
-- END OF CONFIGURATION
-- ==============================================================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rootPart = character:WaitForChild("HumanoidRootPart")
local playerGui = player:WaitForChild("PlayerGui")

local isFarming = false

-- ==================== FUNGSI BANTUAN DASAR ====================
local function getMap()
    return Workspace:FindFirstChild("CurrentRoom") and Workspace.CurrentRoom:FindFirstChildWhichIsA("Model")
end

local function getModelCenter(model)
    if not model or not model:IsA("Model") then return nil end
    local parts = {}
    for _, part in pairs(model:GetDescendants()) do
        if part:IsA("BasePart") then
            table.insert(parts, part)
        end
    end
    if #parts == 0 then return nil end
    local totalPosition = Vector3.zero
    for _, part in pairs(parts) do
        totalPosition = totalPosition + part.Position
    end
    return totalPosition / #parts
end

local function howfar(plrObj, cframe)
    return (plrObj.Character.HumanoidRootPart.Position - cframe.Position).Magnitude
end

local function isMonsterNear(generator, radius)
    local map = getMap()
    if map then
        local monstersFolder = map:FindFirstChild("Monsters")
        if monstersFolder then
            for _, monster in pairs(monstersFolder:GetChildren()) do
                if monster:IsA("Model") and monster.PrimaryPart then
                    local distance = (monster.PrimaryPart.Position - generator.PrimaryPart.Position).Magnitude
                    if distance <= radius then return true end
                end
            end
        end
    end
    return false
end

local function isMachineOccupied(generator, radius)
    local inGamePlayers = Workspace:FindFirstChild("InGamePlayers")
    if inGamePlayers then
        for _, otherPlayer in ipairs(inGamePlayers:GetChildren()) do
            if otherPlayer.Name ~= player.Name then
                local otherRoot = otherPlayer:FindFirstChild("HumanoidRootPart")
                if otherRoot and generator.PrimaryPart then
                    local distance = (otherRoot.Position - generator.PrimaryPart.Position).Magnitude
                    if distance <= radius then return true end
                end
            end
        end
    end
    return false
end

-- ==================== FUNGSI AUTO DETECT (SAFETY DARI REFERENSI) ====================
local function forceStop()
    local decoding = character:FindFirstChild("Decoding") and character.Decoding.Value
    if decoding ~= nil then
        pcall(function()
            decoding.Stats.StopInteracting:FireServer("Stop")
        end)
    end
    -- Pastikan path UI ini sesuai dengan game kamu
    pcall(function()
        return not playerGui.ScreenGui.Menu.StopGenerator.Visible
    end)
    return true
end

local function getFakeElevatorPosition()
    local map = getMap()
    if map then
        local freeArea = map:FindFirstChild("FreeArea")
        if freeArea then
            local fakeElevator = freeArea:FindFirstChild("FakeElevator")
            if fakeElevator and fakeElevator:IsA("Model") then
                return getModelCenter(fakeElevator)
            end
        end
    end
    return nil
end

local function clientinvalidposdetect(playerposition)
    if not playerposition or typeof(playerposition) ~= "Vector3" then
        return false
    end
    if playerposition.Magnitude > 2000 then
        print("[SAFETY] Posisi Magnitudo > 2000 (Terlalu Jauh)")
        return false
    end
    -- Normal Y adalah sekitar 100, jika kurang dari 80 akan memicu teleport
    if playerposition.Y < 80 then
        print("[SAFETY] Jatuh dari map. Posisi Y:", playerposition.Y)
        return false
    end
    if playerposition.X ~= playerposition.X or (playerposition.Y ~= playerposition.Y or playerposition.Z ~= playerposition.Z) then
        return false -- Deteksi NaN
    end
    local v8 = playerposition.X
    if math.abs(v8) ~= (1 / 0) then
        local v9 = playerposition.Y
        if math.abs(v9) ~= (1 / 0) then
            local v10 = playerposition.Z
            if math.abs(v10) ~= (1 / 0) then
                return true
            end
        end
    end
    return false
end

local function teleportplr(cf)
    local originalGravity = Workspace.Gravity
    Workspace.Gravity = 0
    rootPart.AssemblyLinearVelocity = Vector3.zero
    character:PivotTo(cf)
    Workspace.Gravity = originalGravity
end

local function getSproutTantacleCFrames()
    local cframes = {}
    local currentMap = getMap()
    if not currentMap then return cframes end

    local freeArea = currentMap:FindFirstChild("FreeArea")
    if not freeArea then return cframes end

    for _, sproutTantacle in ipairs(freeArea:GetChildren()) do
        if sproutTantacle.Name == "SproutTendril" and sproutTantacle:IsA("Model") then
            table.insert(cframes, sproutTantacle:GetPivot())
        end
    end
    return cframes
end

-- Menjalankan Loop Deteksi Keamanan
task.spawn(function()
    print("[INFO] Memulai Sistem Keamanan Latar Belakang...")
    while true do
        if getMap() and character and rootPart then
            local monstersFolder = getMap():FindFirstChild("Monsters")
            local playerposition = rootPart.Position
            local fakeElevatorPosition = getFakeElevatorPosition()
            
            -- 1. Deteksi jika ketahuan monster dan targetnya adalah localplayer
            if monstersFolder then
                for _, monster in pairs(monstersFolder:GetChildren()) do
                    if monster:FindFirstChild("ChasingValue") and monster.ChasingValue.Value == character then
                        forceStop()
                    end
                end
            end
            
            -- 2. Deteksi jika player jatuh / keluar map
            if not clientinvalidposdetect(playerposition) then
                if fakeElevatorPosition then
                    teleportplr(CFrame.new(fakeElevatorPosition))
                end
            end
            
            -- 3. Deteksi jarak dengan Sprout Tendril
            for _, sproutTantacleCFrame in ipairs(getSproutTantacleCFrames()) do
                local sproutDistance = howfar(player, sproutTantacleCFrame)
                if sproutDistance <= 5 then
                    if fakeElevatorPosition then
                        teleportplr(CFrame.new(fakeElevatorPosition))
                    end
                    break
                end
            end
        end
        task.wait(1/15)
    end
end)

-- ==================== FUNGSI AUTO SKILLCHECK ====================
local function setupAutoSkillcheck()
    if not Config.Skillcheck.Enable then return end
    task.spawn(function()
        while Config.Skillcheck.Enable do
            pcall(function()
                local events = ReplicatedStorage:FindFirstChild("Events")
                if events then
                    local su = events:FindFirstChild("SkillcheckUpdate")
                    if su then
                        su.OnClientInvoke = function() return true end
                    end
                end
            end)
            task.wait(1)
        end
    end)
end

-- ==================== FUNGSI GENERATOR ESP ====================
local function updateGeneratorESP()
    if not Config.ESP.Enable then return end
    local map = getMap()
    if not map then return end
    
    local generatorsFolder = map:FindFirstChild("Generators")
    if generatorsFolder then
        for _, gen in pairs(generatorsFolder:GetChildren()) do
            if gen:IsA("Model") then
                local highlight = gen:FindFirstChild("GenESP")
                if not highlight then
                    highlight = Instance.new("Highlight")
                    highlight.Name = "GenESP"
                    highlight.Parent = gen
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.5
                end
                
                local billboard = gen:FindFirstChild("GenText")
                local label
                if not billboard then
                    billboard = Instance.new("BillboardGui")
                    billboard.Name = "GenText"
                    billboard.Adornee = gen
                    billboard.Size = UDim2.new(0, 100, 0, 50)
                    billboard.AlwaysOnTop = true
                    billboard.Parent = gen
                    
                    label = Instance.new("TextLabel")
                    label.Name = "Text"
                    label.Size = UDim2.new(1, 0, 1, 0)
                    label.BackgroundTransparency = 1
                    label.Font = Enum.Font.Nunito
                    label.TextStrokeTransparency = 0.5
                    label.TextSize = 16
                    label.Parent = billboard
                else
                    label = billboard.Text
                end

                local stats = gen:FindFirstChild("Stats")
                if stats then
                    local completed = stats:FindFirstChild("Completed") and stats.Completed.Value
                    local connie = stats:FindFirstChild("Connie") and stats.Connie.Value
                    local current = stats:FindFirstChild("CurrentAmount") and math.floor(stats.CurrentAmount.Value + 0.5) or 0
                    local required = stats:FindFirstChild("RequiredAmount") and stats.RequiredAmount.Value or 0
                    
                    local genName = gen.Name
                    local textColor = Config.ESP.NormalColor
                    
                    if connie then
                        textColor = Config.ESP.PossessedColor
                        genName = genName .. "\n[POSSESSED]"
                        highlight.FillColor = Config.ESP.PossessedColor
                    elseif completed then
                        textColor = Config.ESP.CompletedColor
                        highlight.FillColor = Config.ESP.CompletedColor
                    else
                        textColor = Config.ESP.NormalColor
                        highlight.FillColor = Config.ESP.NormalColor
                    end
                    
                    label.TextColor3 = textColor
                    label.Text = genName .. "\n(" .. current .. " / " .. required .. ")"
                end
            end
        end
    end
end

-- ==================== FUNGSI INTERAKSI PAKSA ====================
local function forceTriggerGenerator(generator)
    for _, descendant in ipairs(generator:GetDescendants()) do
        if descendant:IsA("ProximityPrompt") then
            descendant.RequiresLineOfSight = false
            descendant.MaxActivationDistance = 100 
            pcall(function()
                descendant:InputHoldBegin()
            end)
            return true
        end
    end
    return false
end

-- ==================== FUNGSI MOVEMENT ====================
local function moveToPosition(targetCFrame, usePlatform)
    local originalGravity = Workspace.Gravity
    Workspace.Gravity = 0
    rootPart.AssemblyLinearVelocity = Vector3.zero
    rootPart.Anchored = false
    
    if Config.Teleport.Mode == "Instant" then
        rootPart.CFrame = targetCFrame
        task.wait(0.1) 
    else
        local speed = player:GetAttribute("KM_MAX_PLAYER_SPEED") or 16
        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
        local duration = distance / (speed * Config.Teleport.TweenSpeed)
        if duration <= 0 then duration = 0.1 end
        
        local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
        local tween = TweenService:Create(rootPart, tweenInfo, { CFrame = targetCFrame })
        tween:Play()
        tween.Completed:Wait()
    end
    
    if usePlatform then
        rootPart.CFrame = targetCFrame
        rootPart.Anchored = true 
    end
    
    Workspace.Gravity = originalGravity
end

-- ==================== FUNGSI TELEPORT KE LIFT ====================
local function teleportToRealElevator()
    local map = getMap()
    if map then
        print("[INFO] Semua Generator Selesai! Mencari Real Elevator...")
        rootPart.Anchored = false 
        
        local freeArea = map:FindFirstChild("FreeArea")
        if freeArea then
            for _, child in ipairs(freeArea:GetChildren()) do
                if child:IsA("Model") and child.Name == "Elevator" and child.PrimaryPart then
                    print("[SUCCESS] Meluncur ke Elevator...")
                    local targetCFrame = child.PrimaryPart.CFrame * CFrame.new(0, 4, 0)
                    moveToPosition(targetCFrame, false)
                    return true
                end
            end
        end
        
        for _, child in ipairs(map:GetChildren()) do
            if child:IsA("Model") and child.Name == "Elevator" and child.PrimaryPart then
                local targetCFrame = child.PrimaryPart.CFrame * CFrame.new(0, 4, 0)
                moveToPosition(targetCFrame, false)
                return true
            end
        end
    end
    return false
end

-- ==================== FUNGSI UTAMA FARMING ====================
local function startAutoFarming()
    task.spawn(function()
        while isFarming do
            if Config.Teleport.Enable then
                local map = getMap()
                if map then
                    local genFolder = map:FindFirstChild("Generators")
                    if genFolder then
                        local targetGen = nil
                        local uncompletedCount = 0
                        
                        for _, gen in pairs(genFolder:GetChildren()) do
                            if gen:IsA("Model") and gen.PrimaryPart then
                                local stats = gen:FindFirstChild("Stats")
                                if stats then
                                    local completed = stats:FindFirstChild("Completed") and stats.Completed.Value
                                    local connie = stats:FindFirstChild("Connie") and stats.Connie.Value
                                    
                                    if not completed then
                                        uncompletedCount = uncompletedCount + 1
                                        if not connie and not targetGen then
                                            if not isMonsterNear(gen, Config.Teleport.SafeRadius) then
                                                if not isMachineOccupied(gen, Config.Teleport.PlayerRadius) then
                                                    targetGen = gen
                                                end
                                            end
                                        end
                                    end
                                end
                            end
                        end
                        
                        if uncompletedCount == 0 then
                            teleportToRealElevator()
                            task.wait(5)
                            
                        elseif targetGen then
                            local centerPos = getModelCenter(targetGen)
                            if centerPos and targetGen.PrimaryPart then
                                local targetCFrame = CFrame.new(centerPos.X, targetGen.PrimaryPart.Position.Y + Config.Teleport.UndergroundOffset, centerPos.Z)
                                
                                moveToPosition(targetCFrame, true)
                                task.wait(0.1)
                                
                                while isFarming and targetGen.Parent do
                                    local stats = targetGen:FindFirstChild("Stats")
                                    if stats and stats:FindFirstChild("Completed") and stats.Completed.Value then
                                        break 
                                    end
                                    
                                    if isMachineOccupied(targetGen, Config.Teleport.PlayerRadius) then
                                        break
                                    end
                                    
                                    rootPart.CFrame = targetCFrame
                                    
                                    local isDecoding = character:FindFirstChild("Decoding") and character.Decoding.Value ~= nil
                                    if not isDecoding then
                                        forceTriggerGenerator(targetGen)
                                    end
                                    
                                    task.wait(0.1)
                                end
                                
                                rootPart.Anchored = false
                                task.wait(Config.Teleport.DelayNext)
                            end
                        end
                    end
                end
            end
            task.wait(1)
        end
    end)
    
    task.spawn(function()
        while isFarming do
            updateGeneratorESP()
            task.wait(0.5)
        end
    end)
end

-- ==================== UI TOGGLE DRAGGABLE SETUP ====================
local guiContainer = pcall(function() return CoreGui end) and CoreGui or player:WaitForChild("PlayerGui")

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FarmToggleUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = guiContainer

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 150, 0, 40)
toggleBtn.Position = UDim2.new(0.5, -75, 0, 20)
toggleBtn.Text = "START FARMING"
toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.Font = Enum.Font.SourceSansBold
toggleBtn.TextSize = 20
toggleBtn.Active = true -- Memungkinkan interaksi geser
toggleBtn.Draggable = true -- Membuat UI bisa digeser dengan bebas
toggleBtn.Parent = screenGui

toggleBtn.MouseButton1Click:Connect(function()
    if isFarming then
        isFarming = false
        Config.Skillcheck.Enable = false
        rootPart.Anchored = false
        toggleBtn.Text = "START FARMING"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    else
        isFarming = true
        Config.Skillcheck.Enable = true
        toggleBtn.Text = "STOP FARMING"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
        setupAutoSkillcheck()
        startAutoFarming()
    end
end)

print("==================================================")
print(" AUTO MACHINE FARMER V10 (SAFETY SYSTEM & DRAGGABLE)")
print(" Loop deteksi bahaya aktif, tombol bisa digeser")
print("==================================================")
