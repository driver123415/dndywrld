-- ==============================================================================
-- KATALOG NAMA ITEM DANDY'S WORLD
-- ==============================================================================
-- [Quest]  "Tape", "ResearchCapsule"
-- [Heal]   "Bandage", "HealthKit"
-- [Mesin]  "JumperCable", "Valve"
-- [Lempar] "SmokeBomb", "EjectButton", "Box chocolates", "ChocolateBox", "AirHorn", "EnigmaCandy", "PopBottle"
-- [Event]  "HolidayCollectibleItem", "Holiday Collectible Item", "FakeCapsule"
-- ==============================================================================

local Config = {
    
    -- [1] PENGATURAN AUTO PICKUP
    Pickup = {
        Enable = true,              
        MovementMode = "Instant",     -- PILIHAN: "Tween" (Aman, meluncur) ATAU "Instant" (Teleport kilat)
        TweenSpeed = 1.25,          -- Kecepatan meluncur jika memakai mode "Tween"
        AuraRange = 30,             
        
        -- 1. PRIORITAS (Diambil duluan dari atas ke bawah)
        PriorityList = {
            "ResearchCapsule",
            "HolidayCollectibleItem",
            "Holiday Collectible Item",
            "Tape"
        },
        
        -- 2. DAFTAR HITAM (TIDAK AKAN diambil, dibiarkan saja di map)
        IgnoreList = {
             "FakeCapsule"
        }
    },

    -- [2] PENGATURAN ESP ITEM
    ESP = {
        Enable = true               -- Ubah ke false jika tidak ingin melihat ESP Item
    },

    -- [3] PENGATURAN AUTO USE ITEM 
    AutoUse = {
        Enable = true,              
        UseInterval = 0.5,          
        Slots = {
            Slot1 = true,
            Slot2 = true,
            Slot3 = true,
            Slot4 = false           
        }
    }
}
-- ==============================================================================
-- END OF CONFIGURATION
-- ==============================================================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rootPart = character:WaitForChild("HumanoidRootPart")

local isAuraActive = false
local isAutoUseActive = false

-- ==================== FUNGSI CEK DAFTAR HITAM ====================
local function isIgnored(itemName)
    for _, ignoreName in ipairs(Config.Pickup.IgnoreList) do
        if itemName == ignoreName then return true end
    end
    return false
end

-- ==================== FUNGSI ITEM ESP ====================
local function applyItemESP()
    if not Config.ESP.Enable then return end
    
    local map = Workspace:FindFirstChild("CurrentRoom") and Workspace.CurrentRoom:FindFirstChildWhichIsA("Model")
    if map and map:FindFirstChild("Items") then
        for _, item in ipairs(map.Items:GetChildren()) do
            if item:IsA("Model") and not item:FindFirstChild("ItemESP") then
                
                -- Buat Highlight
                local highlight = Instance.new("Highlight")
                highlight.Name = "ItemESP"
                highlight.Parent = item
                highlight.FillTransparency = 0.5
                
                -- Warna: Merah jika Blacklist, Hijau jika Target
                if isIgnored(item.Name) then
                    highlight.FillColor = Color3.fromRGB(255, 0, 0) 
                else
                    highlight.FillColor = Color3.fromRGB(0, 255, 0) 
                end
                
                -- Buat Text Label
                local billboard = Instance.new("BillboardGui")
                billboard.Name = "ItemESPText"
                billboard.Adornee = item
                billboard.Size = UDim2.new(0, 100, 0, 50)
                billboard.AlwaysOnTop = true
                billboard.Parent = item
                
                local label = Instance.new("TextLabel")
                label.Size = UDim2.new(1, 0, 1, 0)
                label.BackgroundTransparency = 1
                label.Text = item.Name
                label.TextColor3 = highlight.FillColor
                label.TextStrokeTransparency = 0.5
                label.TextSize = 14
                label.Parent = billboard
            end
        end
    end
end

-- ==================== FUNGSI AUTO USE ITEM ====================
local function startAutoUse()
    if not Config.AutoUse.Enable then return end
    isAutoUseActive = true
    
    task.spawn(function()
        while isAutoUseActive do
            local char = player.Character
            if char and char:FindFirstChild("Inventory") then
                for slotName, isActive in pairs(Config.AutoUse.Slots) do
                    if isActive then
                        local slotItem = char.Inventory:FindFirstChild(slotName)
                        if slotItem then
                            pcall(function()
                                ReplicatedStorage.Events.ItemEvent:InvokeServer(char, slotItem)
                            end)
                        end
                    end
                end
            end
            task.wait(Config.AutoUse.UseInterval)
        end
    end)
end

-- ==================== FUNGSI MOVEMENT & AURA ====================
local function getMap()
    return Workspace:FindFirstChild("CurrentRoom") and Workspace.CurrentRoom:FindFirstChildWhichIsA("Model")
end

local function interactWithPrompt(prompt)
    if prompt.HoldDuration == 0 then
        prompt:InputHoldBegin()
        prompt:InputHoldEnd()
    else
        prompt:InputHoldBegin()
        task.wait(prompt.HoldDuration)
        prompt:InputHoldEnd()
    end
end

local function startItemAura()
    isAuraActive = true
    task.spawn(function()
        while isAuraActive do
            local map = getMap()
            if map and rootPart then
                local itemsFolder = map:FindFirstChild("Items")
                if itemsFolder then
                    for _, item in pairs(itemsFolder:GetChildren()) do
                        if not isIgnored(item.Name) then
                            local promptPart = item:FindFirstChild("Prompt")
                            if promptPart then
                                local proximityPrompt = promptPart:FindFirstChild("ProximityPrompt")
                                if proximityPrompt and proximityPrompt.Enabled then
                                    local distance = (promptPart.Position - rootPart.Position).Magnitude
                                    if distance <= Config.Pickup.AuraRange then
                                        interactWithPrompt(proximityPrompt)
                                    end
                                end
                            end
                        end
                    end
                end
            end
            task.wait(0.05)
        end
    end)
end

-- Fungsi bergerak yang mendukung Tween dan Instant Teleport
local function moveToPosition(targetCFrame)
    local originalGravity = Workspace.Gravity
    Workspace.Gravity = 0
    rootPart.AssemblyLinearVelocity = Vector3.zero
    
    if Config.Pickup.MovementMode == "Instant" then
        -- Mode Instant Teleport
        rootPart.CFrame = targetCFrame
        task.wait(0.1) -- Jeda stabilisasi teleport
    else
        -- Mode Tween
        local speed = player:GetAttribute("KM_MAX_PLAYER_SPEED") or 16
        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
        local duration = distance / (speed * Config.Pickup.TweenSpeed)
        
        local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
        local tween = TweenService:Create(rootPart, tweenInfo, { CFrame = targetCFrame })
        tween:Play()
        tween.Completed:Wait()
    end
    
    Workspace.Gravity = originalGravity
end

-- ==================== FUNGSI UTAMA AUTO PICKUP ====================
local function executePickup()
    if not Config.Pickup.Enable then return end
    
    local map = getMap()
    if not map then return end
    local itemsFolder = map:FindFirstChild("Items")
    if not itemsFolder or #itemsFolder:GetChildren() == 0 then return end

    -- Nyalakan ESP agar terlihat jelas mana yang target dan mana yang ignore
    applyItemESP()

    local originalPosition = rootPart.CFrame
    startItemAura()
    
    local validItems = {}
    for _, item in ipairs(itemsFolder:GetChildren()) do
        if item:IsA("Model") and not isIgnored(item.Name) then
            table.insert(validItems, item)
        end
    end

    -- Fungsi bantuan untuk memproses item dengan validasi habis
    local function processItem(item)
        if item and item.Parent then
            print("[PICKUP] Bergerak ke: " .. item.Name)
            moveToPosition(item:GetPivot() * CFrame.new(0, -3.3, 0))
            
            -- VALIDASI: Tunggu sampai item benar-benar hilang (Max 25x cek = 2.5 detik)
            local timeout = 25 
            while item.Parent and timeout > 0 do
                task.wait(0.1)
                timeout = timeout - 1
            end
            
            if item.Parent then
                print("[WARNING] Gagal/Lama mengambil: " .. item.Name .. ". Melanjutkan ke item berikutnya...")
            else
                print("[SUCCESS] " .. item.Name .. " berhasil diambil!")
            end
        end
    end

    -- LANGKAH 1: Ambil item PRIORITAS terlebih dahulu
    for _, priorityName in ipairs(Config.Pickup.PriorityList) do
        for i = #validItems, 1, -1 do
            local item = validItems[i]
            if item.Name == priorityName and item.Parent then
                processItem(item)
                table.remove(validItems, i) 
            end
        end
    end

    -- LANGKAH 2: Ambil sisa item yang TIDAK ADA di prioritas maupun blacklist
    for _, item in ipairs(validItems) do
        if item.Parent then
            processItem(item)
        end
    end

    print("[INFO] Kembali ke posisi awal...")
    moveToPosition(originalPosition)
    
    task.wait(0.2)
    isAuraActive = false
    print("[DONE] Auto Pickup Selesai!")
end

-- ==================== JALANKAN SCRIPT ====================
startAutoUse()
executePickup()
