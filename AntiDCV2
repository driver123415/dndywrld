local GuiService = game:GetService("GuiService")

GuiService.ErrorMessageChanged:Connect(function(errorMessage)
    -- Jika terjadi error/kick, jangan teleport. 
    -- Tulis laporan 'DC_BOS' ke file agar dibaca oleh Python!
    if writefile then
        writefile("status_bot.txt", "DC_BOS")
    end
end)
