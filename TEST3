-- ==============================================================================
-- CONFIGURATION: AUTO MACHINE FARMER V2 (UNDERGROUND & INSTANT)
-- ==============================================================================
local Config = {
    
    -- [1] PENGATURAN AUTO SKILLCHECK
    Skillcheck = {
        Enable = true,          -- Otomatis menyelesaikan skillcheck dengan Perfect
    },

    -- [2] PENGATURAN TELEPORT & POSISI
    Teleport = {
        Enable = true,          
        Mode = "Instant",       -- PILIHAN: "Tween" (meluncur) atau "Instant" (teleport kilat)
        TweenSpeed = 1.25,      -- Kecepatan jika memakai mode Tween
        SafeRadius = 30,        -- Jarak aman dari monster
        DelayNext = 1.5,        -- Jeda sebelum pindah ke mesin berikutnya
        
        -- PENGATURAN SEMBUNYI DI BAWAH TANAH
        UndergroundOffset = -7  -- Posisi karakter di bawah mesin (-7 studs ke bawah tanah)
    },

    -- [3] PENGATURAN GENERATOR ESP
    ESP = {
        Enable = true,          
        NormalColor = Color3.fromRGB(150, 150, 150),   
        CompletedColor = Color3.fromRGB(255, 0, 255),  
        PossessedColor = Color3.fromRGB(0, 255, 255)   
    }
}
-- ==============================================================================
-- END OF CONFIGURATION
-- ==============================================================================

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local rootPart = character:WaitForChild("HumanoidRootPart")

local isFarming = false

-- ==================== MEMBUAT PLATFORM PIJAKAN ====================
-- Membuat lantai melayang tak terlihat agar karakter tidak jatuh ke void
local farmPlatform = Instance.new("Part")
farmPlatform.Name = "FarmPlatform"
farmPlatform.Size = Vector3.new(10, 1, 10)
farmPlatform.Anchored = true
farmPlatform.CanCollide = true
farmPlatform.Transparency = 1 -- Ubah ke 0.5 jika kamu ingin melihat pijakannya
farmPlatform.Material = Enum.Material.SmoothPlastic
farmPlatform.Parent = nil -- Akan dimasukkan ke workspace saat mulai farming

-- ==================== FUNGSI BANTUAN ====================
local function getMap()
    return Workspace:FindFirstChild("CurrentRoom") and Workspace.CurrentRoom:FindFirstChildWhichIsA("Model")
end

local function isMonsterNear(generator, radius)
    local map = getMap()
    if map then
        local monstersFolder = map:FindFirstChild("Monsters")
        if monstersFolder then
            for _, monster in pairs(monstersFolder:GetChildren()) do
                if monster:IsA("Model") and monster.PrimaryPart then
                    local distance = (monster.PrimaryPart.Position - generator.PrimaryPart.Position).Magnitude
                    if distance <= radius then
                        return true
                    end
                end
            end
        end
    end
    return false
end

-- ==================== FUNGSI AUTO SKILLCHECK (V2 BYPASS) ====================
local function setupAutoSkillcheck()
    if not Config.Skillcheck.Enable then return end
    task.spawn(function()
        print("[INFO] Memulai Auto Skillcheck Bypass (V2)...")
        while Config.Skillcheck.Enable do
            pcall(function()
                local events = ReplicatedStorage:FindFirstChild("Events")
                if events then
                    local su = events:FindFirstChild("SkillcheckUpdate")
                    if su then
                        su.OnClientInvoke = function() 
                            return true 
                        end
                    end
                end
            end)
            task.wait(1)
        end
    end)
end

-- ==================== FUNGSI GENERATOR ESP ====================
local function updateGeneratorESP()
    if not Config.ESP.Enable then return end
    local map = getMap()
    if not map then return end
    
    local generatorsFolder = map:FindFirstChild("Generators")
    if generatorsFolder then
        for _, gen in pairs(generatorsFolder:GetChildren()) do
            if gen:IsA("Model") then
                local highlight = gen:FindFirstChild("GenESP")
                if not highlight then
                    highlight = Instance.new("Highlight")
                    highlight.Name = "GenESP"
                    highlight.Parent = gen
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.5
                end
                
                local billboard = gen:FindFirstChild("GenText")
                local label
                if not billboard then
                    billboard = Instance.new("BillboardGui")
                    billboard.Name = "GenText"
                    billboard.Adornee = gen
                    billboard.Size = UDim2.new(0, 100, 0, 50)
                    billboard.AlwaysOnTop = true
                    billboard.Parent = gen
                    
                    label = Instance.new("TextLabel")
                    label.Name = "Text"
                    label.Size = UDim2.new(1, 0, 1, 0)
                    label.BackgroundTransparency = 1
                    label.Font = Enum.Font.Nunito
                    label.TextStrokeTransparency = 0.5
                    label.TextSize = 16
                    label.Parent = billboard
                else
                    label = billboard.Text
                end

                local stats = gen:FindFirstChild("Stats")
                if stats then
                    local completed = stats:FindFirstChild("Completed") and stats.Completed.Value
                    local connie = stats:FindFirstChild("Connie") and stats.Connie.Value
                    local current = stats:FindFirstChild("CurrentAmount") and math.floor(stats.CurrentAmount.Value + 0.5) or 0
                    local required = stats:FindFirstChild("RequiredAmount") and stats.RequiredAmount.Value or 0
                    
                    local genName = gen.Name
                    local textColor = Config.ESP.NormalColor
                    
                    if connie then
                        textColor = Config.ESP.PossessedColor
                        genName = genName .. "\n[POSSESSED]"
                        highlight.FillColor = Config.ESP.PossessedColor
                    elseif completed then
                        textColor = Config.ESP.CompletedColor
                        highlight.FillColor = Config.ESP.CompletedColor
                    else
                        textColor = Config.ESP.NormalColor
                        highlight.FillColor = Config.ESP.NormalColor
                    end
                    
                    label.TextColor3 = textColor
                    label.Text = genName .. "\n(" .. current .. " / " .. required .. ")"
                end
            end
        end
    end
end

-- ==================== FUNGSI INTERAKSI PAKSA (BYPASS PROMPT) ====================
local function forceTriggerGenerator(generator)
    for _, descendant in ipairs(generator:GetDescendants()) do
        if descendant:IsA("ProximityPrompt") then
            -- Bypass jarak dan penghalang objek (Line of Sight) agar bisa ditekan dari bawah tanah
            local oldLineOfSight = descendant.RequiresLineOfSight
            local oldMaxDist = descendant.MaxActivationDistance
            
            descendant.RequiresLineOfSight = false
            descendant.MaxActivationDistance = 50
            
            -- Tekan tombol
            descendant:InputHoldBegin()
            task.wait(0.1)
            descendant:InputHoldEnd()
            
            -- Kembalikan settingan asli
            descendant.RequiresLineOfSight = oldLineOfSight
            descendant.MaxActivationDistance = oldMaxDist
            return true
        end
    end
    return false
end

-- ==================== FUNGSI PERGERAKAN ====================
local function moveToPosition(targetCFrame)
    local originalGravity = Workspace.Gravity
    Workspace.Gravity = 0
    rootPart.AssemblyLinearVelocity = Vector3.zero
    
    -- Pindahkan Platform pijakan ke bawah kaki target CFrame
    farmPlatform.CFrame = targetCFrame * CFrame.new(0, -3.1, 0)
    farmPlatform.Parent = Workspace
    
    if Config.Teleport.Mode == "Instant" then
        rootPart.CFrame = targetCFrame
        task.wait(0.2) -- Jeda stabilisasi setelah teleport kilat
    else
        local speed = player:GetAttribute("KM_MAX_PLAYER_SPEED") or 16
        local distance = (rootPart.Position - targetCFrame.Position).Magnitude
        local duration = distance / (speed * Config.Teleport.TweenSpeed)
        
        local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut)
        local tween = TweenService:Create(rootPart, tweenInfo, { CFrame = targetCFrame })
        tween:Play()
        tween.Completed:Wait()
    end
    
    Workspace.Gravity = originalGravity
end

-- ==================== FUNGSI UTAMA FARMING ====================
local function startAutoFarming()
    isFarming = true
    
    -- Loop 1: Teleport & Eksekusi Mesin
    task.spawn(function()
        while isFarming do
            if Config.Teleport.Enable then
                local map = getMap()
                if map then
                    local genFolder = map:FindFirstChild("Generators")
                    if genFolder then
                        local targetGen = nil
                        
                        -- 1. Cari mesin target
                        for _, gen in pairs(genFolder:GetChildren()) do
                            if gen:IsA("Model") and gen.PrimaryPart then
                                local stats = gen:FindFirstChild("Stats")
                                if stats then
                                    local completed = stats:FindFirstChild("Completed") and stats.Completed.Value
                                    local connie = stats:FindFirstChild("Connie") and stats.Connie.Value
                                    
                                    if not completed and not connie then
                                        if not isMonsterNear(gen, Config.Teleport.SafeRadius) then
                                            targetGen = gen
                                            break
                                        end
                                    end
                                end
                            end
                        end
                        
                        -- 2. Jika mesin ditemukan, pindah dan kerjakan
                        if targetGen then
                            print("[FARMER] Menuju Generator: " .. targetGen.Name)
                            local genCF = targetGen.PrimaryPart.CFrame
                            
                            -- Set target di BAWAH TANAH dari titik tengah mesin
                            local targetCFrame = genCF * CFrame.new(0, Config.Teleport.UndergroundOffset, 0)
                            
                            moveToPosition(targetCFrame)
                            
                            -- 3. Loop terus selesaikan mesin ini sampai kelar
                            while isFarming and targetGen.Parent do
                                local stats = targetGen:FindFirstChild("Stats")
                                if stats and stats:FindFirstChild("Completed") and stats.Completed.Value then
                                    print("[FARMER] Generator Selesai!")
                                    break -- Keluar dari loop mesin ini, cari mesin lain
                                end
                                
                                -- Jaga-jaga pastikan karakter tetap nempel di target (anti lag/geser)
                                if (rootPart.Position - targetCFrame.Position).Magnitude > 3 then
                                    rootPart.CFrame = targetCFrame
                                end
                                
                                -- Cek apakah kita sedang "Decoding" (Mengerjakan mesin)
                                local isDecoding = character:FindFirstChild("Decoding") and character.Decoding.Value ~= nil
                                
                                -- Jika belum Decoding, paksa tekan tombol
                                if not isDecoding then
                                    forceTriggerGenerator(targetGen)
                                end
                                
                                task.wait(0.5)
                            end
                            
                            task.wait(Config.Teleport.DelayNext)
                        end
                    end
                end
            end
            task.wait(1)
        end
    end)
    
    -- Loop 2: Update ESP
    task.spawn(function()
        while isFarming do
            updateGeneratorESP()
            task.wait(0.5)
        end
    end)
end

-- ==================== INISIALISASI ====================
print("==================================================")
print(" AUTO MACHINE FARMER V2 BERHASIL DIJALANKAN")
print("==================================================")
setupAutoSkillcheck()
startAutoFarming()
