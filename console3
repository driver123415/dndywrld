--NEW CONSOLE MOBILE auto scroll always on OLD
local screen = Instance.new("ScreenGui")
screen.IgnoreGuiInset = false
screen.ResetOnSpawn = true
screen.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screen.Name = "Screen"
screen.Parent = game.CoreGui

--// Tombol untuk membuka (Unminimize) console
local openBtn = Instance.new("TextButton")
openBtn.Size = UDim2.new(0, 120, 0, 35)
openBtn.Position = UDim2.new(0, 10, 0, 10)
openBtn.BackgroundColor3 = Color3.new(0.078, 0.121, 0.152)
openBtn.TextColor3 = Color3.new(1, 1, 1)
openBtn.Font = Enum.Font.SourceSansBold
openBtn.TextSize = 16
openBtn.Text = "Open Console"
openBtn.Visible = false
openBtn.Parent = screen

local obCorner = Instance.new("UICorner")
obCorner.CornerRadius = UDim.new(0, 8)
obCorner.Parent = openBtn

--// Frame Utama Console
local console = Instance.new("Frame")
console.BackgroundColor3 = Color3.new(0.0784314, 0.121569, 0.152941)
console.Position = UDim2.new(0.358, 0, 0.264, 0)
console.Size = UDim2.new(0.4, 0, 0.5, 0)
console.Visible = true
console.Active = true -- Diperlukan untuk drag
console.Name = "Console"
console.Parent = screen

local uicorner = Instance.new("UICorner")
uicorner.Parent = console

local uistroke = Instance.new("UIStroke")
uistroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
uistroke.Color = Color3.new(1, 1, 1)
uistroke.Thickness = 2.4
uistroke.Parent = console

local uigradient = Instance.new("UIGradient")
uigradient.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.new(0.0862745, 0, 0.72549)), ColorSequenceKeypoint.new(1, Color3.new(0.294118, 0.380392, 0.435294))})
uigradient.Parent = uistroke

--// Judul Console (Tengah Atas)
local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, -40, 0, 30) -- Dikurangi sedikit agar tidak menabrak tombol minimize
titleLabel.Position = UDim2.new(0, 0, 0, 10)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "CONSOLE"
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.TextSize = 22
titleLabel.TextXAlignment = Enum.TextXAlignment.Center
titleLabel.Parent = console

--// Tombol Minimize (-) - Dipindahkan ke posisi yang lebih estetis
local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(1, -35, 0, 10) -- Diturunkan sedikit
minimizeBtn.BackgroundColor3 = Color3.new(0.8, 0.2, 0.2)
minimizeBtn.TextColor3 = Color3.new(1, 1, 1)
minimizeBtn.Font = Enum.Font.SourceSansBold
minimizeBtn.Text = "-"
minimizeBtn.TextScaled = true
minimizeBtn.Parent = console

local minCorner = Instance.new("UICorner")
minCorner.CornerRadius = UDim.new(1, 0)
minCorner.Parent = minimizeBtn

--// Logic Minimize & Unminimize
minimizeBtn.MouseButton1Click:Connect(function()
	console.Visible = false
	openBtn.Visible = true
end)

openBtn.MouseButton1Click:Connect(function()
	console.Visible = true
	openBtn.Visible = false
end)

--// Resize Handle (Pojok kanan bawah untuk memperbesar/kecil)
local resizeHandle = Instance.new("TextButton")
resizeHandle.Size = UDim2.new(0, 20, 0, 20)
resizeHandle.Position = UDim2.new(1, -20, 1, -20)
resizeHandle.BackgroundColor3 = Color3.new(1, 1, 1)
resizeHandle.BackgroundTransparency = 0.8
resizeHandle.Text = "⌟"
resizeHandle.TextColor3 = Color3.new(1, 1, 1)
resizeHandle.TextSize = 20
resizeHandle.Parent = console

--// Scrolling Frame untuk Logs
local console_2 = Instance.new("ScrollingFrame")
console_2.CanvasSize = UDim2.new(0, 0, 0, 0)
console_2.ScrollBarImageColor3 = Color3.new(0.137255, 0.211765, 0.266667)
console_2.ScrollBarThickness = 8
console_2.Active = true
console_2.BackgroundColor3 = Color3.new(0.101961, 0.101961, 0.101961)
console_2.BackgroundTransparency = 1
console_2.BorderSizePixel = 0
console_2.Position = UDim2.new(0.02, 0, 0.15, 0) -- Diturunkan agar tidak menutupi tombol minimize
console_2.Size = UDim2.new(0.96, 0, 0.82, 0) -- Disesuaikan
console_2.Visible = true
console_2.Name = "ConsoleList"
console_2.Parent = console

local logs = Instance.new("Folder")
logs.Name = "Logs"
logs.Parent = console_2

local uilist_layout = Instance.new("UIListLayout")
uilist_layout.Padding = UDim.new(0, 10) -- Padding antar log lebih besar
uilist_layout.SortOrder = Enum.SortOrder.LayoutOrder
uilist_layout.HorizontalAlignment = Enum.HorizontalAlignment.Center -- Log akan di tengah frame
uilist_layout.Parent = logs

--// Template Frame Log (Example) - Perbaikan Total
local example = Instance.new("Frame")
example.BackgroundColor3 = Color3.new(1, 1, 1)
example.BackgroundTransparency = 1
example.Size = UDim2.new(1, -15, 0, 30) -- Ukuran fixed untuk list layout
example.Visible = false -- Disembunyikan agar template asli tidak terlihat
example.Name = "Example"
example.Parent = logs

-- Label Baris ("1 - ") - Dipisahkan ke frame sendiri
local lineLabel = Instance.new("TextLabel")
lineLabel.Font = Enum.Font.Code -- Font monospace lebih rapi
lineLabel.Text = "1 - "
lineLabel.TextColor3 = Color3.new(0.282353, 0.282353, 0.282353)
lineLabel.TextSize = 14
lineLabel.BackgroundColor3 = Color3.new(1, 1, 1)
lineLabel.BackgroundTransparency = 1
lineLabel.Position = UDim2.new(0, 5, 0, 0)
lineLabel.Size = UDim2.new(0, 30, 1, 0) -- Lebar wadah label baris
lineLabel.TextXAlignment = Enum.TextXAlignment.Left -- Teks di kiri
lineLabel.Visible = true
lineLabel.Name = "LineLabel"
lineLabel.Parent = example

-- Label Teks Log Utama - Diberi ruang besar dan pembungkusan
local logLabel = Instance.new("TextLabel")
logLabel.Font = Enum.Font.Cartoon -- Sesuai permintaan
logLabel.Text = "Log: Hello, World"
logLabel.TextColor3 = Color3.new(0.423529, 0.8, 1)
logLabel.TextSize = 14
logLabel.BackgroundColor3 = Color3.new(1, 1, 1)
logLabel.BackgroundTransparency = 1
logLabel.Position = UDim2.new(0, 35, 0, 0) -- Di kanan label baris
logLabel.Size = UDim2.new(1, -110, 1, 0) -- Disesuaikan agar muat tombol copy
logLabel.TextXAlignment = Enum.TextXAlignment.Left -- Teks di kiri
logLabel.TextYAlignment = Enum.TextYAlignment.Center -- Teks di tengah vertikal
logLabel.TextWrapped = true -- Teks terbungkus ke baris baru
logLabel.TextScaled = false -- Jangan diskalakan agar keterbacaan terjaga
logLabel.Visible = true
logLabel.Name = "LogLabel"
logLabel.Parent = example

--// Tombol Copy di dalam log - Tetap sama
local copyBtn = Instance.new("TextButton")
copyBtn.Size = UDim2.new(0, 50, 0, 22)
copyBtn.Position = UDim2.new(1, -55, 0.5, -11)
copyBtn.BackgroundColor3 = Color3.new(0.18, 0.55, 0.34)
copyBtn.TextColor3 = Color3.new(1, 1, 1)
copyBtn.Font = Enum.Font.SourceSansBold
copyBtn.TextSize = 13
copyBtn.Text = "Copy"
copyBtn.Name = "CopyBtn"
copyBtn.Parent = example

local cpCorner = Instance.new("UICorner")
cpCorner.CornerRadius = UDim.new(0, 4)
cpCorner.Parent = copyBtn


--// Dragging Logic (Geser GUI)
local UIS = game:GetService("UserInputService")
local dragging, dragInput, dragStart, startPos

console.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = console.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragging = false end
		end)
	end
end)

console.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UIS.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart
		console.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)


--// Resizing Logic (Perbesar/Kecil GUI)
local resizing, resizeStart, startSize

resizeHandle.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		resizing = true
		resizeStart = input.Position
		startSize = console.Size
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then resizing = false end
		end)
	end
end)

UIS.InputChanged:Connect(function(input)
	if resizing and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
		local delta = input.Position - resizeStart
		console.Size = UDim2.new(startSize.X.Scale, startSize.X.Offset + delta.X, startSize.Y.Scale, startSize.Y.Offset + delta.Y)
	end
end)


--// Hook & Core Script
task.spawn(function()
	local Logs = logs
	local ExampleFrame = example
	local LineCount = 0

	local CreateMessage = function(...)
		LineCount += 1
		if LineCount >= 250 then
			local oldLog = Logs:FindFirstChild("Line: " .. tostring(LineCount - 250))
			if oldLog then oldLog:Destroy() end
		end
		
		local msgText = ...
		local NewFrame = ExampleFrame:Clone()
		NewFrame.Name = "Line: " .. tostring(LineCount)
		NewFrame.LayoutOrder = LineCount
		NewFrame.Visible = true
		
		-- Merujuk ke struktur template yang baru
		local line = NewFrame:FindFirstChild("LineLabel")
		local txt = NewFrame:FindFirstChild("LogLabel")
		local copyBtn = NewFrame:FindFirstChild("CopyBtn")

		if line and txt and copyBtn then
			line.Text = tostring(LineCount) .. " - "
			txt.Text = tostring(msgText)
			
			-- Fungsi Copy untuk log ini
			copyBtn.MouseButton1Click:Connect(function()
				if setclipboard then
					setclipboard(tostring(msgText))
				end
			end)
		end

		NewFrame.Parent = Logs
		
		-- Auto-scroll mengikuti log terbaru
		console_2.CanvasSize = UDim2.new(0, 0, 0, uilist_layout.AbsoluteContentSize.Y + 10)
		console_2.CanvasPosition = Vector2.new(0, console_2.CanvasSize.Y.Offset)
	end

	local function tableToString(tbl)
		local result = {}
		local function traverseTable(t, indent)
			for k, v in pairs(t) do
				if type(v) == "table" then
					result[#result + 1] = indent .. tostring(k) .. ": {"
					traverseTable(v, indent .. "  ")
					result[#result + 1] = indent .. "}"
				else
					result[#result + 1] = indent .. tostring(k) .. ": " .. tostring(v)
				end
			end
		end
		traverseTable(tbl, "")
		return table.concat(result, "\n")
	end

	local prt
	prt = hookfunction(print, function(...)
		local args = ...
		if type(args) == "table" then
			args = tableToString(args)
		elseif type(args) == "function" then
			args = getupvalues(args)
		end
		local message = tostring(args)
		CreateMessage(message)
		return warn(message) -- Sesuai dengan behavior aslinya
	end)
end)
